services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        NEXT_PUBLIC_BASE_PATH: /sisdm
        GIT_COMMIT: ${GIT_COMMIT:-unknown}
    restart: unless-stopped
    env_file:
      - .env.production
    environment:
      UPLOADS_DIR: /app/uploads
    ports:
      - "127.0.0.1:3500:3500"
    volumes:
      - ${UPLOADS_HOST_DIR:-/mnt/nas/informasijp/uploads}:/app/uploads
    depends_on:
      mariadb:
        condition: service_healthy
    command: npm run start
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1:3500/sisdm/api/health"]
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 30s

  migrate:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        NEXT_PUBLIC_BASE_PATH: /sisdm
        GIT_COMMIT: ${GIT_COMMIT:-unknown}
    env_file:
      - .env.production
    depends_on:
      mariadb:
        condition: service_healthy
    command: npm run prisma:migrate:deploy
    profiles:
      - migrate

  mariadb:
    image: mariadb:10.4.27
    restart: unless-stopped
    env_file:
      - .env.production
    volumes:
      - mariadb_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "127.0.0.1", "-uroot", "-p$${MYSQL_ROOT_PASSWORD}"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s

volumes:
  mariadb_data:
